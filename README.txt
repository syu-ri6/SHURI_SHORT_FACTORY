SHURI SHORT STUDIO GitHub upload package

Upload every file in this folder to the SAME repository directory as index.html.
Do not rename the bgm_*.mp3 files unless you also update BGM_DATA in index.html.

PANIC / run away from the enemy is intentionally not included because no source file was attached.
The app will show it as missing instead of substituting another track.
